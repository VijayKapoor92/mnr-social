import React from 'react';
import classNames from 'classnames';
import Typography from '@material-ui/core/Typography';
import './index.css';

const Spacer = () =>
    <Typography className={classNames('spacer')} />

export default Spacer